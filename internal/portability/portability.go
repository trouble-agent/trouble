// Package portability holds the source-invariant guard that keeps Linux-only
// code paths behind build constraints (TRBL-002). It has no runtime code: the
// whole point is the test next to it.
package portability
