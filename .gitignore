bin/
dagger.db
*.db
*.db-shm
*.db-wal
*.prof
.scrub-e2e-*
.scrub-ingest-*
*.test
dist/

.sentinel-e2e-*
.sentinel-load-*
.sentinel-dbg-*
.worktrees/
*.db
.coding-hermes/board/*.db
.coding-hermes/board/*.parquet
.coding-hermes/board/wal/
.coding-hermes/board/snapshots/
.gitreins/tasks.yaml
.gitreins/history/
.gitreins/verdicts/
.gitreins/usage.jsonl

# Container secret material (mounted at run time, never committed).
deploy/container/secrets/

# Local runtime scratch that must never be swept into a commit:
# DuckBrain write staging (nested repo with its own .git) and GitReins run logs.
namespaces/
.gitreins/logs/
